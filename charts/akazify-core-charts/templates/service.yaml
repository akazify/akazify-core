apiVersion: v1
kind: Service
metadata:
  name: {{ include "akazify-core-charts.fullname" . }}
  labels:
    {{- include "akazify-core-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "akazify-core-charts.selectorLabels" . | nindent 4 }}
